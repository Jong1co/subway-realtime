export interface StationService {
  line: string;
  nextStation: string;
  // direction: string;
  arrivalTime: number;
  currentStation: string;
  lastStation: string;
  arrivalState: string;
  // bookmark: boolean;
  // currentSubwayPositions: string[];
}
