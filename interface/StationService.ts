export interface StationService {
  line: string;
  nextStation: string;
  // direction: string;
  arrivalTime: number;
  currentStation: string;
  lastStation: string;
  // bookmark: boolean;
  // currentSubwayPositions: string[];
}
